} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
warnings()
tidier.range
tidier.range <- ((i* 180) : (i + 1 * 180))
tidier.range
i
tidier <- matrix(nrow=180*66, ncol=10)
names(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', "Magnitude",
"Jerl", 'Direction')
tidier <- as.data.frame(tidier)
for (i in 3:68) {
tidier.range <- ((i* 180) : ((i + 1) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
length(tidy$activity)
tidier <- matrix(nrow=180*66, ncol=10)
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', "Magnitude",
"Jerl", 'Direction')
tidier <- as.data.frame(tidier)
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', "Magnitude",
"Jerl", 'Direction')
tidier$matrix(nrow=180*66, ncol=10)
matrix(nrow=180*66, ncol=10)
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerl", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- ((i* 180) : ((i + 1) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- ((i* 180) : ((i + 1) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- (((i - 2)* 180) : ((i - 1) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- (((i - 3)* 180) : ((i - 2) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
warnings()
length(tidy$subject)
length(tidier.range)
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- (((i - 3)* 181) : ((i - 2) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- (((i - 3)* 180 + 1) : ((i - 2) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name]
}
length(v)
length(tidier.range)
str(tidier)
tidier$Average[1]
tidier$Subject[1]
tidier$Subject[10]
tidier$Subject[180]
tidier$Subject[181]
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- (((i - 3)* 180 + 1) : ((i - 2) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- as.numeric(tidy[name])
}
tidy[name]
tidy[name][1]
tidy[name][,1]
class(tidy[name][,1])
class(tidy[name][[,1]])
class(tidy[name][[1]])
tidy[name][[1]]
tidier <- as.data.frame(matrix(nrow=180*66, ncol=10))
colnames(tidier) <- c('Subject', 'Activity', 'Domain', 'Signal',
'Instrument', 'Variable', 'Magnitude',
"Jerk", 'Direction', 'Average')
for (i in 3:68) {
tidier.range <- (((i - 3)* 180 + 1) : ((i - 2) * 180))
name <- colnames(tidy)[i]
if (grepl("Frequency", name)) {
tidier$Domain[tidier.range] <- "Frequency"
} else {
tidier$Domain[tidier.range] <- "Time"
}
if (grepl("Body", name)) {
tidier$Signal[tidier.range] <- "Body"
} else {
tidier$Signal[tidier.range] <- "Gravity"
}
if (grepl("Accelerometer", name)) {
tidier$Instrument[tidier.range] <- "Accelerometer"
} else {
tidier$Instrument[tidier.range] <- "Gyroscope"
}
if (grepl("mean", name)) {
tidier$Variable[tidier.range] <- "Mean"
} else {
tidier$Variable[tidier.range] <- "Standard.deviation"
}
if (grepl("Jerk", name)) {
tidier$Jerk[tidier.range] <- TRUE
} else {
tidier$Jerk[tidier.range] <- FALSE
}
if (grepl("Magnitude", name)) {
tidier$Magnitude[tidier.range] <- TRUE
} else {
tidier$Magnitude[tidier.range] <- FALSE
}
if (grepl("-Z", name)) {
tidier$Direction[tidier.range] <- "Z"
} else if (grepl("-X", name)) {
tidier$Direction[tidier.range] <- "X"
} else if (grepl("-Y", name)) {
tidier$Direction[tidier.range] <- "Y"
}
tidier$Subject[tidier.range] <- tidy$subject
tidier$Activity[tidier.range] <- tidy$activity
tidier$Average[tidier.range] <- tidy[name][[1]]
}
str(tidier)
summary(tidier)
tidier$Magnitude
tidier$Direction
write.table(tidier, "tidy_data.txt", row.names=FALSE)
